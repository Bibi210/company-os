[package]
name = "companyos-crash-trace"
version.workspace = true
edition.workspace = true

[dependencies]
companyos-config = { path = "../config" }
chrono = { workspace = true }

[dev-dependencies]
tempfile = "3"
